#include <iostream>
using namespace std;
vector<int> reverse(vector<int>) {
    

    arr[] = {1,2,3,4,5,6} ;
    int start = arr[0],end = arr[n-1];
    for (i=0; i<n-1; i++){
       if(start != end){
        swap(start,end);
       }
       if else (start > end){
        return ;
       }
       else {
        return ;
       }
    }
}
int main(){
    vector<int> v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);
    v.push_back(4);
    v.push_back(5);
    v.push_back(6);

    return 0;
}