#include <iostream>
using namespace std;

private:
  bool valid(char ch){
    if ((ch >= 'a' && ch <= 'z')||(ch>='A' && ch <= 'Z')||(ch >= '0' && ch <='9'))
     return 1;
  }
  char toLowerCase(char ch){
    if(ch >='')
  }